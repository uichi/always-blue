---
date: "2021-11-21 20:00"
updated_date: "2021-11-21 20:00"
title: "export defaultと変数宣言は同時にできない"
description: ""
categories: ["React"]
tags: ["構文エラー"]
---

>warning  Assign arrow function to a variable before exporting as module default  import/no-anonymous-default-export

# 原因
`export default`を関数に直接指定していたため、構文エラーが出ました。

```tsx
export default const Hello() => {
  console.log('Hello World')
};
```

正しくは、こうです。


```tsx
const Hello() => {
  console.log('Hello World')
};

export default Hello;
```