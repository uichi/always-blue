---
title: React実装などでたまに出てくるドット3つの意味
date: "2019-10-31 06:39"
updated_date: "2021-11-14 22:00"
description: JavaScriptの実装でたまに見かけるドット3つの意味を解説します
categories: ["JavaScript"]
tags: ["スプレッド構文", "React"]

hero_image_alt: ""
hero_image_credit_text: ""
hero_image_credit_link: ""
---
Reactをやっているとたまにドットの3つ付いた記法を見かけることがあります。

この記法は子コンポーネントへ値を渡したいときなどに使われます。

## 例
```
const props = { foo: 'foo', hoge: 'hoge' };
render() {
  return <Component {...props} />
}
```

ドット3つを使わない場合はこのようにかきます。
```
const props = { foo: 'foo', hoge: 'hoge' };
render() {
  return <Component foo={props.foo} hoge={hoge} />
}
```

## 参考
[React での「...」（ドット3つ）の記法](https://yami-beta.hateblo.jp/entry/2016/09/06/112330)
